import ListConversions from './ListConversions'
import ViewConversion from './ViewConversion'
const Pages = {
    ListConversions: Object.assign(ListConversions, ListConversions),
ViewConversion: Object.assign(ViewConversion, ViewConversion),
}

export default Pages