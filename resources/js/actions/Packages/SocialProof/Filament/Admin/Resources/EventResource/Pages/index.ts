import ListEvents from './ListEvents'
import ViewEvent from './ViewEvent'
const Pages = {
    ListEvents: Object.assign(ListEvents, ListEvents),
ViewEvent: Object.assign(ViewEvent, ViewEvent),
}

export default Pages