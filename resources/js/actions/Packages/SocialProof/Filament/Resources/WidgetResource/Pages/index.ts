import ListWidgets from './ListWidgets'
import CreateWidget from './CreateWidget'
import ViewWidget from './ViewWidget'
import EditWidget from './EditWidget'
const Pages = {
    ListWidgets: Object.assign(ListWidgets, ListWidgets),
CreateWidget: Object.assign(CreateWidget, CreateWidget),
ViewWidget: Object.assign(ViewWidget, ViewWidget),
EditWidget: Object.assign(EditWidget, EditWidget),
}

export default Pages